#!/usr/bin/env bash
set -euo pipefail

mkdir -p "$HOME/Pictures/Screenshots"
FILE="$HOME/Pictures/Screenshots/$(date +%F-%H%M%S).png"

grim -g "$(slurp -o)" "$FILE"
wl-copy < "$FILE"
notify-send "Window screenshot saved" "$FILE copied to clipboard"
