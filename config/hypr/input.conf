input {
  kb_layout = sk,us
  kb_options = grp:alt_shift_toggle
  follow_mouse = 1
  sensitivity = 0

  touchpad {
    natural_scroll = false
    tap-to-click = true
  }
}

device {
  name = epic-mouse-v1
  sensitivity = -0.5
}
